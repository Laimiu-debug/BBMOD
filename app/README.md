# BBMOD · 战团整备所

面向《战场兄弟》1.5.2.3 的本地桌面工具：营地诊断、MOD 军械库、独立汉化工坊与种子远征。

## 下载测试版

当前已发布软件为 **0.3.0-rc.5 测试版**，独立汉化沿用 rc.2：[下载 EXE 与查看已知问题](https://github.com/Laimiu-debug/BBMOD/releases/tag/v0.3.0-rc.5)。源码 rc.6 已完成刷种子自动开局修复与离线验证，但旧中文地名启动组件被 Windows Defender 隔离，EXE 构建尚未完成。隔离测试副本的中文启动问题和游戏内验收仍未完成，已按用户要求停止游戏测试。

## 在线军械库

进入「MOD 军械库 → 在线军械库」，填入站长提供的网站根地址并连接。支持搜索作者作品、查看说明、下载校验并安装；更新时备份旧版，保留启用/禁用状态，可恢复上一版。未受管理或已手动修改的同名 ZIP 不会被覆盖。前置与冲突按 MOD ID 检查，版本范围和 DLC 要求需阅读作者说明。网站部署见 [web/README.md](../web/README.md)。

## 版本管理与自动更新

点击右上角「版本与更新」：查看当前软件版本、内置独立汉化版本、GitHub 发布历史及每版说明。Windows 文件属性也包含软件版本。旧版 rc.3 及更早版本没有更新器，需要先手动下载 rc.4 并替换一次 EXE。

- 默认开启启动后后台检查；距离上次成功检查不足 6 小时则复用版本记录，软件持续运行时每 6 小时检查一次，也可随时点击「立即检查」。可以关闭自动检查。
- 支持「稳定版」与「稳定版 + 测试版」通道。本版为测试版，默认包含测试版；稳定版通道没有发行版时会明确提示，不会把测试版误报为稳定版。
- 发现较新版本后，右上角显示提示。检查失败、断网或 GitHub 限流不会阻塞主界面，也不会被显示成“已经最新”。
- 选择版本可阅读说明、打开发布页面或下载 EXE。下载支持取消，并校验发布资产的大小、SHA-256 和 Windows 程序标识；校验失败不允许安装。
- 下载较新版本后，发行版可点击「重启并更新」。更新器等待旧软件退出，在原位置替换 EXE 并重启。替换或进程启动失败会尝试恢复旧版；旧版备份以 `BBMOD.previous-xxxxxxxx.exe` 保存在程序旁边。程序正常启动后的业务行为仍需另行验证。
- 正在刷种子、构建汉化或处理 MOD 时禁止重启更新。设置和下载记录存于 `%APPDATA%/BBMOD`，汉化与 MOD 配置不随 EXE 替换。已有汉化 ZIP 不会被自动重新生成。
- 历史版本可以下载留存；软件不会自动降级。源码运行时可检查和下载，安装按钮仅在 Windows 打包版中启用。无目录写权限时可打开下载目录手动处理。

更新检查使用公开的 [GitHub Releases API](https://docs.github.com/en/rest/releases/releases)，不需要登录。发布说明按纯文本展示。重启使用 [PyInstaller 的独立进程环境](https://pyinstaller.org/en/stable/common-issues-and-pitfalls.html#using-sys-executable-to-spawn-subprocesses-that-outlive-the-application-process-implementing-application-restart)，避免继承已退出程序的解包目录。

开发检查：`python -m pytest tests/test_app_updates.py tests/test_update_network.py`；`python tools/check_updates.py --download` 会在临时配置中检查公开版本并下载校验；`python tools/check_update_install.py --exe <新版EXE> --old-exe <旧版EXE>` 只更新隔离的软件副本，运行软件自检，不会打开游戏。

## 设置汉化并启动游戏

打开 `dist/BBMOD.exe`，选择游戏目录，进入「汉化工坊 → 汉化管理与启动」：

1. 已有其他汉化时，选择「从已安装文件添加」，把同一套汉化的本体和专用字体保存为一个方案；下载的新汉化用「导入汉化 ZIP」添加，可多选。通用框架和其他 MOD 在军械库中管理。
2. 使用本项目汉化时，点击「生成 / 更新独立汉化」。独立译文编辑位于旁边的页签，生成的包会加入内置方案。
3. 选择方案，查看即将停用、启用的文件，以及覆盖相同游戏文件的冲突 MOD；点击「应用方案」或「应用并启动」。停用文件移至 `bbmod_disabled`，同名旧文件保留副本，不覆盖原包。
4. 平时打开软件，直接点击顶部「启动游戏」即可。未应用的方案会要求先核对切换预览；游戏已经运行时不能切换或重复启动。

BBMOD 独立汉化自动加载随 EXE 内置的中文地图字体；其他汉化沿用其自己的文件和游戏设置。启动指向当前选定目录的 `win32/BattleBrothers.exe`，不会改用另一个 Steam 安装目录。

### 地名随启动方式切换

使用 **0.3.0-rc.2** 的 EXE，先生成并应用一次新版独立汉化包：

| 启动方式 | 地图、任务和对话中的地名 | 其他正文 |
| --- | --- | --- |
| BBMOD 的「启动游戏」或「应用并启动」 | 中文 | 中文 |
| Steam 或游戏 EXE 直接启动 | 英文 | 中文 |

仅打开 BBMOD 不会改变已经运行的游戏。通过 BBMOD 启动后，可以关闭软件，本次游戏继续显示中文地名；下次直接启动仍显示英文。原始名称和存档始终使用英文，城镇、区域、营地和传奇地点共用经过校对的显示词库。营地名称中的随机人名使用原版顺序的英文姓名库，人物自身的汉化不变。

旧存档已经保存的中文地名不会因换包自动恢复。新规则使用离线脚本和界面检查验证；后续启动尝试未能进入游戏，个人存档校验未变化。第三方汉化仍遵循其自身行为。

方案和包副本保存在对应游戏目录的 `bbmod_localizations`，切换前生成校验过的备份。写入失败会恢复；中断后可点击「恢复上次切换」。种子远征未恢复时禁止切换和普通启动。开发测试副本的存档保护文件不参与汉化切换。

本功能管理 ZIP 形式的汉化。合集需要先解压并选取实际汉化包；直接修改过的 `.dat` 或额外安装器写入的文件不能靠停用 ZIP 自动还原。第三方包只作为完整文件管理，不导入其译文和脚本到独立词库。

当前功能已在临时目录验证切换、失败恢复、界面布局及启动分流。第三方汉化的实际运行兼容性尚未验收。

## 独立汉化的实际范围

当前版本 **0.3.0-rc.2** 的本体与官方 DLC 已提取目录共 **15,629 条文本**，涵盖 2,319 个来源文件：**13,433 条非地名文本及 2,196 条地名译文均已独立精修**。地名在显示层切换语言，原始英文数据不变。合并 186 条补充界面文本后，汉化工坊展示 **15,815 条文本**。正文目录为 `localization/full_catalog.json`，独立地名精修表为 `localization/reviewed_place_names.json`，均随 `dist/BBMOD.exe` 分发；预生成包为 `dist/mod_bbmod_zhcn.zip`。

- 源码与词库：`localization/catalog.json`、`full_catalog.json`、`runtime.js`、`fonts.css`。
- 基础包从官方归档读取 `ui/main.html` 和原版输入控件，注入自有显示适配器，并修补中文输入法的按键和内容更新事件。
- 不读取狐狸汉化 ZIP，不导入其译文、Squirrel 脚本、修改器或 hooks。
- 无需另装 MOD Hooks/MSU；为兼容已有玩法 MOD，随包提供公开的 Modding Script Hooks 21.1 运行文件并附署名。保留原版 Squirrel 指令、随机数调用和存档格式。地名生成函数中的两个姓名库引用指向自有配置中的原版英文数组，顺序及重复值不变；其余运行标识保持原样。正文只替换经过检查的显示文本常量。
- 界面使用 Noto Sans SC，地图使用 Noto Serif SC SemiBold，均附各自的 SIL OFL 许可证和来源记录。地图字体由本项目的适配器绘制，保留原来的位置和颜色，使用细描边并去除矩形底板。
- 正文和地名均显示「已校对」。地名精修表覆盖旧目录中的地名初稿，构建时展开为 7,723 个可显示名称，地图和对话使用相同结果；不会写回游戏名称或存档。全目录变量、数字、排版标记、换行、随机选句分隔符及分支内变量对应关系均通过检查，快捷键、品牌与制作人员署名按规则保留。
- 当前精修包完成离线结构和文本检查，后续隔离副本的启动尝试失败，游戏内验收仍待进行；以前的小范围实机截图不能替代新版验收，也不能据此保证所有游戏运行路径都没有问题。已按用户要求停止游戏测试。

译文目录在未安装游戏时也能编辑。自定义译文保存在 `%APPDATA%/BBMOD/l10n_independent_overrides.json`，不会导入旧版第三方脚本字段覆盖表。

在「汉化工坊」导出 ZIP，或者使用：

```powershell
python tools/build_l10n.py --game 'E:/SteamLibrary/steamapps/common/Battle Brothers' --output dist/mod_bbmod_zhcn.zip
```

命令只生成文件，不安装。界面中的「生成并选择此汉化」把构建结果加入汉化方案；核对切换预览并点击「应用」后，才会停用列出的旧汉化或冲突 MOD，再安装所选包。已经修改过的游戏基座需要先自行还原。

### 当前离线验证与历史开发测试入口

2026-09-15 的测试副本位于 `app/build/full-l10n/game-check`。桌面的「BBMOD 独立汉化测试（开发中）」固定通过 `tools/launch_preview.py` 启动此副本；缺少测试文件时会报错，不会跳转到 Steam 的常用游戏目录。游戏窗口标题也会标明独立测试身份。

当前版本的离线验证包含 **2,281 个 Squirrel 字节码文件的原生读取、19,329 个原版函数的指令保持、205,339 个受保护常量检查，以及 44 个 JavaScript 文件的语法检查**。新增的独立地名查询脚本与包内框架一起在离线解释器中运行检查。受保护常量仅允许两个已核对的地名姓名库引用变更；公开 MOD 框架的四个文件单独做字节校验。之后仅向隔离测试副本更新了包和存档保护文件，常用游戏目录未更新，个人存档校验未变化。启动尝试未进入游戏，已停止游戏测试。

`localization/reviewed_refinement.json` 累计收录 **11,127 条独立修订文本及片段**，不能与完整目录数量相加。101 项离线回归通过；原生离线解释器执行包内代码，核对了 153 条完整特长及动态提示、3,800 个原版英文地名生成样例。地名专项检查覆盖 7,723 个地图及对话名称、直接启动→软件启动→再次直接启动、1 万次原生接口模拟调用和原始文字不变的绘制副本。显星 MOD 启用和停用、中文输入法与动态界面文本分别通过离线检查。详情见 [精修记录](localization/REFINEMENT.md) 和 [验证记录](localization/VALIDATION.md)。

`localization/reviewed_initial.json` 收录先前独立编写的 1,284 条修订片段和术语，纠正漏句、错误数字、人物和怪物名称、无关叙述及不自然的短句。片段数和完整条目数不能相加。

`localization/reviewed_onboarding.json` 收录 102 条独立校对的开场、创建战役、DLC、地图提示和术语修订，包括 16 种起源说明、5 项 DLC 说明和新生战团的完整开场剧情。构建时若这些已校对文本被筛选器遗漏，将直接报错。

`localization/reviewed_equipment_contracts.json` 收录 763 条独立重译与校对文本。按实际补丁引用核对，覆盖武器目录的 352 条、商队护送委托的 79 条、共用议价模板的 42 条及劫掠者背景的 14 条（条目跨文件复用，数量不能相加）。同时修正多种委托选项的措辞，保留原版随机文本分隔符两侧空格，避免多个选项句子挤在同一按钮上。委托回调返回的页面 ID 和表中的 ID/Type 字段保持原文；共享的可见标题通过显示层汉化。

2026-09-15 的实机检查已通过起源选择、中文战团名、难度选择、开场事件和地图五个页面：中文输入正常，实际按钮能开始战役，生成 3 名成员和 20 座聚落，开场全文及按钮为中文，地图地名无矩形底框，日志无脚本错误。截图与包哈希见 [验证记录](localization/VALIDATION.md)。这只验证上述流程，不代表全部文本已译完。

2026-09-16 较早的部分汉化包曾实机通过投矛提示、劫掠者招募身世、商队任务介绍、议价和确认页，日志无脚本错误；原版引擎的 2,336 次随机选项检查全部通过。测试到达接受报价后的委托确认页，未覆盖完整护送路程、战斗及结算。这些历史检查对应旧包，不能作为当前精修包的实机结果。

测试不自动进入地图，请新建战役。由于不同游戏副本共用系统文档中的存档目录，测试包用单独的保护文件禁用 `autosave` 和 `saveCampaign`，不保存测试进度。专用入口会核对保护文件与汉化包的对应关系。此保护仅存在于开发测试副本，不属于正式汉化包。

初译和独立精修均已完成。模型和缓存只保存在 `build/full-l10n`，不会随汉化包或桌面工具加载，游戏运行不需要翻译模型。开发用初译工具若检测到游戏启动，会释放显存并等待游戏关闭。

## 界面与图标

四个工作台采用木质底板、铆钉金属边框、卷轴面板与暗红旗帜选中态。
手绘风格营地插画与头盔盾徽由内置 ImageGen 生成，已随应用打包；盾徽同时用于窗口、任务栏与 EXE，导航和操作按钮使用金属徽章图标。
素材与生成提示词见 `assets/art-direction.json`。
中文字体与 Cinzel 标题字体随应用分发，避免依赖目标机器的字体配置。`Alt+1–4` 可切换工作台。

## MOD 与刷种子

### 显星、属性及评价 MOD 兼容

独立汉化不再打包 `world_town_screen_hire_dialog_module.js`，由原 MOD 保留自己的招募界面和回调，常用界面词语通过独立显示层翻译。随包提供原有 seedgen 资源中的 Modding Script Hooks 21.1 四个运行文件；不使用框架旧版的 `ui/main.html`，也不从狐狸汉化提取脚本。框架署名与来源随 ZIP 提供。

未启用显星 MOD 时，游戏使用原版招募界面，显示正常汉化的人物背景、雇佣费用及原版按钮；考察仍按原版规则揭示特性，不额外显示星级、属性预估、评价或移除按钮。只有启用该 MOD 后，才显示它提供的额外内容。汉化包本身不包含显星脚本。当前分发包已分别通过启用和停用两种状态的离线界面检查。

原显星 MOD 在招募时可能把属性记录追加到人物身世，并随人物存档保存。停用后，这类旧记录可能仍留在已经招募的人物身上；软件不会自动清除它们。本轮没有读取或修改存档。

`tools/check_star_rating.py` 离线执行本机实际「显星显属性带评价（影响存档）」脚本和界面，检查星级、属性、预估值、评价、中文背景、招募/考察/移除回调及新战役钩子重置。对本机 6 个 ZIP 的完整副本模拟切换到独立汉化，保留全部 5 个玩法 MOD，冲突为零；正式游戏目录只读。该检查不是游戏内验收，也不证明其余 MOD 的独立依赖全部满足。

切回原有狐狸汉化时，它与显星 MOD 原有的招募界面覆盖仍会在切换预览中提示；本轮没有修改第三方汉化或隐藏此冲突。

MOD 管理包括仓库、启停和集合方案。诊断结合脚本静态线索和上次游戏日志，不能证明任意第三方 MOD 完全兼容。

种子远征启动后，仍需在游戏中选择实际起源、难度并开始新战役。文件会话保存在游戏根目录 `bbmod_seedgen_session/`：

1. 备份所有启用的 MOD，以及即将覆盖的已有种子配置和脚本。
2. 写入持久化清单后，再移除 MOD、注入种子生成器。
3. 停止后按 SHA-256 校验恢复原文件，删除本次新建的注入文件。
4. 程序中断后，再次开始远征会先恢复上次会话。
5. 会话期间意外变化的文件另存到 `bbmod_seedgen_session/preserved/`。

备份校验失败时保留清单并报告错误。旧版 `bbmod_seedgen_stash/` 遗留目录需要先检查恢复，不能声称已自动可靠修复旧版崩溃现场。
停止远征后，结果仍可查看、复制、导出 TXT。双击种子行可打开完整档案。

### 筛选条件

默认按明确属性筛选：例如至少3名兄弟，同时满足11级预估近战命中≥90、近战防御≥25；选择“不限”的属性不参与筛选。生命、决心、疲劳上限等门槛放在“其他属性”中。地图页可直接设置港口、城镇、甲店数量，红装页设置全地图红装数量；只有当前寻找模式涉及的条件页可用。

在「开局兄弟 → 属性与特质 → 特质筛选」可搜索原版 58 种开局特质，为每项选择「不限」「必须具备」或「不能拥有」。必选项可以要求全部具备，也可允许任意一项；与能力值组合时，计入人数的每名兄弟必须同时满足整套条件。排除项只限制这些兄弟，不会因其他队员拥有该特质而淘汰整个种子。

例如：至少 1 人，近战命中 ≥90、近战防御 ≥25，全部具备「高大（巨人）」与「铁肺」，并排除「笨拙」。想忽略能力值时选择「仅按特质筛选」。互斥特质的全部匹配会提示修正；起源可能限制某些特质出现，不能保证任意组合都有种子。沿用起源预设和高级评分模式使用各自原有规则，不附加此特质面板的选择。

特质选择会保存；结果的种子档案按人物显示中文特质，TXT 导出保留这些详情。「只找开局兄弟（快）」已修正为执行人物筛选分支。此版本 111 项离线回归通过，其中实际 Squirrel 判定器执行 16 个样例检查同一人物匹配、任意／全部、排除、人数和门槛边界；没有启动游戏。

11级数值按每级提升该属性计算，逐级模拟或星级平均值均为预估；不同属性同时成长仍受实际加点选择影响。

### 自动开局与汉化

在软件里选好起源和条件，点击「开始远征」后会启动当前游戏目录，等待主菜单加载就绪，再自动按所选起源新建战役。无需进游戏再选一遍，也不依赖固定秒数或鼠标位置。起源不可用或游戏实际设置不符时明确报错；进度区显示等待菜单、加载战役、生成中及错误状态。

「生成设置」可选战斗难度、经济难度、初始资金，默认老兵／老兵／中。起源和这些设置会保存，并写入结果详情及弹幕 TXT；复现红装请使用相同起源、难度、游戏版本和 DLC。软件只接收本次会话标记之后的日志，不会混入上次刷种子的记录。

刷种子准备阶段会备份并暂时移出独立汉化和其他已启用的 ZIP/RAR MOD，结束后恢复；官方 DAT 文件不改动。独立汉化包不参与这次生成。若修改直接合并进 DAT，移出 MOD 无法撤销，但仅凭 ZIP 条目日期无法判断是否汉化、是否重打包或是否改变生成逻辑。原先“汉化重打包、种子失真”的日期误判弹窗已移除，日期仅在营地诊断里作为信息显示。

自动开局经过离线 Squirrel 验证，使用本机提取的原游戏菜单脚本覆盖 14 个起源、42 组设置及失败分支；未启动游戏，实际游戏内流程仍待验收。

“队伍平均分”保留在高级评分中：每名兄弟按职业名额分配职业，汇总其评分后取平均。个人评分综合初始属性、11级预估属性与特性；近战权重为命中35%、近防30%、疲劳20%、生命10%、决心5%。分数可以低于0或超过1，0.8不能直接换算为某项属性或百分位。评分条件按严格大于比较，属性条件按大于等于比较。完整解释可在界面点击“评分说明”。

### TXT、复制与弹幕模式

- 输出格式可选“种子档案”“弹幕模式（一行一条）”“仅种子码”，复制与 TXT 导出使用当前所选格式。
- 点击“复制所选”复制当前行；开启“点选即复制”后，单击种子行即可复制，格式和开关会记住。新结果加入时不自动改动剪贴板。
- 弹幕开场白可选“卡大货了？”“缺大哥？”等，也可自填。自动亮点只使用日志记录的港口、预估属性和红装数量。
- 金鹅位置、乘船路线等可在“补充介绍 / 路线”手动填写，按种子码与实际起源保存。程序不会凭空生成未采集的地标和行走路线。
- TXT 使用带 BOM 的 UTF-8，保留种子大小写；没有命中结果时禁用导出。

## 开发与验证

```powershell
pip install -r requirements.txt
python main.py
python main.py --selftest
python -m pytest tests tools/seedgen_check.py -k 'not orchestrator_roundtrip' -q
python tools/ui_check.py
python tools/check_localization_manager_ui.py
```

`tests/` 使用临时游戏目录，覆盖独立汉化构建、安装冲突、译文筛选编辑、停止后导出、配置恢复、进程中断恢复、写入失败和备份损坏。
`tools/ui_check.py` 以只读方式显示本机游戏状态，生成 1360×880 和 1080×720 的四页截图到 `build/review/`。
`tools/check_localization_manager_ui.py` 使用明确标记的临时示例包，在相同两种尺寸检查汉化管理与编辑页的按钮位置；不会启动游戏或修改本机游戏安装。

显示适配器的 DOM 验证使用 Node.js 和 jsdom：

```powershell
npm --prefix build/browser-check install --no-save --no-package-lock jsdom
node tests/runtime.test.cjs ./build/browser-check/node_modules/jsdom
```

注意：旧的 `tools/manager_check.py` 和 `tools/seedgen_check.py` 直接执行模式会操作真实游戏目录；它们不属于上述默认隔离测试流程。

## 打包

安装 `requirements-dev.txt` 后：

```powershell
python tools/build_icons.py
cmd /c native\build.cmd
python -m PyInstaller --noconfirm BBMOD.spec
python tools/package_check.py
```

当前 spec 保留单文件构建，成品 `dist/BBMOD.exe`，包含完整独立精修词库、字体、图标、种子脚本和中文地图字体组件。内置 MOD 资源合集仍从 EXE 周边仓库目录寻找，不把第三方汉化并入应用。

地名专项检查：先构建本地预览 ZIP，再运行 `python tools/check_place_display.py`。该工具运行独立的原生测试 EXE、离线 Squirrel 解释器及 Node.js，不启动游戏。原生调用约定及存档边界见 `native/place_session_abi.md`。

打包时将依赖搜索范围限定在 Python 与 Windows 目录，避免其他工具的同名 DLL 混入。`package_check.py` 检查 EXE 内资源，并使用临时用户配置运行桌面自检；不会安装汉化或启动游戏。

## 来源与说明

- 视觉参考：[开发者介绍的木材与金属界面方向](https://battlebrothersgame.com/dev-blog-74-progress-update-reworked-ui/)。营地插画与盾徽由 ImageGen 生成，操作徽章与边框由本项目绘制，未复制游戏美术。
- [Noto Sans SC](https://github.com/google/fonts/tree/main/ofl/notosanssc) 与 [Cinzel](https://github.com/google/fonts/tree/main/ofl/cinzel) 字体使用 SIL OFL 许可，许可证与字体一同分发。
- 内置第三方 MOD 合集与本项目独立汉化是不同资源；相关原始文件仍保留其原有来源。
