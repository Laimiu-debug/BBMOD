# BBMOD 种子广场 · Hub 0.3.0

2026-09-17 升级现有 Django 源站，Vercel 网关与域名继续沿用原配置。

- 公开页面：`https://bbmod.site/seeds/`。
- 发布目录：`/home/ubuntu/bbmod-hub/releases/BBMOD-Hub-0.3.0-96b16244800d`。
- 源码包 SHA-256：`96b16244800db7dce86a851ab150cefd2371f4c157f95e3c9199d0df43de25f5`，95 个文件。
- 升级前备份：`/home/ubuntu/bbmod-hub/backups/20260917-seeds-030-96b16244800d/`，包含数据库、上传文件、程序版本、环境配置和容器基线。敏感配置不放进源码包。
- 备份副本迁移演练通过；逐行比较原账号、MOD、MOD 版本、管理器版本均未变化。迁移仅新增种子分享、人物索引与限流表。
- 容器内 37 项测试通过、迁移文件无漂移；运行中数据库完整性检查为 `ok`。
- 只更新 `bbmod-hub-app-1`，原 BBMOD 网关和其他业务容器的 ID、启动时间保持不变。
- `bbmod.site` 和 `bbmod.vercel.app` 的广场、CSS、JS 均返回 200，原下载页和 MOD API 保持 200。两个域名的匿名上传端点均收到版本化 JSON 请求，并对无效空档案返回 400；未向生产网站写入测试种子。
- 本地完整流程通过：桌面 HTTP 客户端匿名上传 → 档案持久化 → 起源筛选 → 详情 → 复制种子码。浏览器检查覆盖 1440 像素与 390 像素，未出现横向溢出和控制台错误。测试数据仅在隔离的本地数据库。

生产环境保留 1 个 MOD、5 个管理器版本。rc.7 EXE 是本地测试包，本次没有将其发布到 GitHub 或网站下载列表。

回退应用时，可在此前 `BBMOD-Hub-0.2.0-cb53d8abce7c/web` 目录使用同一 Compose 项目仅切回 app；新增种子表与旧程序兼容。不要直接覆盖运行数据库，避免丢失升级后的分享；如需数据恢复，先停写并另行备份当前数据库。
