# 访客编号与访问统计 · Hub 0.3.1

2026-09-17 已部署到 `https://bbmod.site/`，MOD 首页、下载页、种子广场和公开详情页显示「欢迎你，第 xxx 位好兄弟」及累计人数。

- 发布目录：`/home/ubuntu/bbmod-hub/releases/BBMOD-Hub-0.3.1-035b73d78fd6`。
- 源码包：`app/build/hub/0.3.1/BBMOD-Hub-0.3.1.tar.gz`，102 个文件；SHA-256 `035b73d78fd6174cd2a8888152ad1288e25ffa6180ef84502bfeb4f772e19380`。
- 备份：`/home/ubuntu/bbmod-hub/backups/20260917-visitors-031-3d597b9473eb/`。这是本次升级开始时的数据库、上传文件、配置和容器基线备份，修正页面边距后沿用同一份未变动的升级前备份。
- 迁移 `0004_site_visitors` 仅新增访客表。备份副本演练比较原账号、MOD、程序版本、种子与人物数据，全部保持不变。
- 只重建 BBMOD app 容器；网关、Vercel 配置、域名及其他业务服务没有改变。
- 本地和 Linux 发布镜像均通过 44 项测试，覆盖签名 Cookie、刷新去重、不同浏览器编号、CSRF、爬虫排除、统计故障、今日统计和 12 次并发访问计数。
- 浏览器验证覆盖公开欢迎语、跨页面编号不变、两个独立浏览器分别领号、后台汇总，以及 1440 / 390 像素布局。手机页面没有横向溢出。公网首次访问和刷新显示相同编号；另起进程查询运行数据库确认计数已持久化。
- 公网 HTML / JS / CSS 与原下载页、MOD API 均正常。生产数据库仍保留原 MOD 和桌面版本；没有导入本地测试数据。

统计从此次上线开始，不推算此前的访问。以浏览器 Cookie 估算访客，不等于自然人数量；清除站点数据、跨浏览器、设备或域名、Cookie 过期会重新计数。不开启 JavaScript 的访问不参与计数。数据库只存随机标识的哈希、编号、首次/最近访问时间与浏览次数；不保存 IP 和逐页浏览轨迹。后台展示累计访客、今日访客、今日新访客、累计浏览次数。

回退可使用此前 Hub 0.3.0 发布目录切换 app 镜像；新访客表与旧程序兼容。不要覆盖数据库或删除数据卷，以免丢失新增访客与分享。
