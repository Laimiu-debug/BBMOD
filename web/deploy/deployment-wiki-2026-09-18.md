# 战场兄弟百科 · Hub 0.3.3

2026-09-18 已上线：https://bbmod.site/wiki/ 。官网主导航增加「游戏百科」，管理后台增加百科导入报告。

## 资料与功能

- 原站主命名空间全部 2,063 篇正文、192 个重定向；保存 133 个模板、92 个分类页和 5,387 个文件说明页的原始版本。另按原站成员关系生成 24 个分类目录。
- 来源清单与保存修订均为 7,867 条；全部正文同时有原始 Wikitext 和清洗后的阅读内容。正文缺失、正文缺图、内部重定向断链和原站解析警告均为零。
- 缓存 5,366 条文件记录，去重后发布 5,101 个图片文件，共 254,383,722 字节。图片、正文和搜索在访问时均不依赖 Fandom。
- 中文与英文搜索；双语名称、英文原文两种阅读模式；目录、分类分页、重定向和锚点；手机端表格横向滚动；保留合并单元格、公式与原站过时提示；事件内容折叠。
- BBMOD zh 词库对应游戏 1.5.2.3，选用 3,727 条已校对名称，1,883 个页面包含名称对照。英文正文完整保留，当前不是全文汉化。
- 原文版本、贡献历史、原始文本下载、正文许可及文件来源可查。开放 `/api/v1/wiki/search/` 和 `/api/v1/wiki/pages/<id>/`，供后续桌面百科使用。

快照：`20260918T123041Z-62ef6dcfb10d`。词库 SHA-256：

`1cc25078e8a60fc69d38d878b1f04bb4dd17bbc03433141557c69ec47bda3956`

## 发布与数据保护

在已核验的线上 `BBMOD-Hub-0.3.2-47843a7c094d` 基础上，只覆盖百科所需的官网文件。工作区其他桌面端、种子及汉化修改没有混入此次发布包。

发布目录：`/home/ubuntu/bbmod-hub/releases/BBMOD-Hub-0.3.3-f3c6890d0bbb`。

源码包：`output/deployment/wiki-20260918/BBMOD-Hub-0.3.3.tar.gz`，132 个文件，SHA-256：

`f3c6890d0bbb45fed8e54c64318c648dabef10475b3a9fe6166016dc274f397e`

百科资料包：`output/deployment/wiki-20260918/wiki-20260918T123041Z-62ef6dcfb10d.tar.gz`，271,956,881 字节，SHA-256：

`8ce878b800a7f0481529af85c064a5ccd09dc98102fb0f2288fa2510b4ad5c1c`

资料存于原有持久卷的 `/srv/data/wiki/`，源数据与社区数据库独立。快照、图片上传后逐项验证哈希及数据库完整性，再原子切换资料指针。未增加社区数据库迁移。

升级前备份：`/home/ubuntu/bbmod-hub/backups/20260918-wiki-f3c6890d0bbb/`。备份 SHA-256：

`27e2692436c5f8158da7b7581512f6e9bffdde5624fdeac23f95ac1c18996a1a`

备份副本演练确认所有原有行保留，12 个下载文件逐项核对大小和 SHA-256。上线后仍有 2 个账号、1 个 MOD、2 条 MOD 版本、10 条管理器版本、10 条分享种子及 21 条种子人物索引。

上线后额外执行新版 `backup_hub`：`/srv/data/backups/bbmod-hub-20260918T124539720257Z.tar.gz`。已核对其中百科指针、快照文件及全部 5,101 个图片哈希。

只替换 `bbmod-hub-app-1`。已逐个核对 BBMOD 网关及服务器其他容器的 ID 和启动时间，均未变化；没有调整 Vercel、DNS、网关路由，没有提交或推送 Git。公网继续沿用现有 `private, no-store` 网关策略，包括静态路径；应用层的内容哈希图片缓存声明不会改变当前网关策略。

回退应用可启用前一发布目录和镜像，仅替换 app。百科资料保留于持久卷，可独立回退快照。不要恢复旧社区数据库覆盖上线后新增的建议或访客记录。

## 验收证据

- 工作区、隔离发布源码和 Linux 发布镜像的 70 项 Django 测试通过，包含 12 项百科测试；`makemigrations --check --dry-run` 无漂移。
- 覆盖中英文检索、重定向与锚点、名称对照、分类、剧透折叠、原始文本和来源、私有报告权限、只读快照、发布完整性、图片路径、HTML/CSS 清洗、MathML 以及未被正文引用的文件预览。
- 扫描 7,891 个已编译页面，没有活动脚本、外部图片请求地址或缺失的本地图片引用。
- 正式域名 15 个页面、接口及静态资源返回 200，包括下载、种子、提建议、百科和两个公开目录 API。
- 正式浏览器实际完成「巨剑搜索 → Greatsword 词条 → 英文模式」，验证装备 16 个表格与 8 个公式、人物背景 81 个表格、游戏机制和事件 391 个表格；手机 390 像素下页面不横向溢出，长表可单独横向滚动。
- 浏览器验收屏蔽外部资源后通过；零外部资源请求、零加载失败、零脚本异常。首次快速连续切页出现请求中断，改为等待资源加载完成后复查全部通过。记录保存在 `output/deployment/wiki-20260918/browser-wiki-acceptance.txt`、`browser-public-check.txt`，截图在 `output/playwright/wiki-*-accepted.png`。
- 正式库中的匿名建议提交、去重及管理员处理用事务执行后整体回滚，没有留下测试建议；数据库完整性为 `ok`。

## 已知来源边界

- 5 处正文视频保留来源链接；20 个视频、无二进制记录或不支持的文件仅保存说明。`Bandages.png` 对应的原站地址返回 404，文件页明确说明。正文使用的配图没有缺失。
- 原站有 4 个不同的缺失游戏词条目标，BBMOD 显示带搜索入口的缺页说明；管理维护页和用户博客保留原站入口。论坛、聊天、账号及用户页面不在此次游戏百科范围内。
- 原站媒体元数据没有提供明确的独立许可声明，记录为 `source_unstated` 并展示来源，没有把图片认定为正文 CC 许可授权。正文和其名称、排版改编按 CC BY-SA 3.0 提供，图片权利仍需独立核实。
- Fandom CDN 可能把 PNG、JPEG 或 GIF 转为 WebP；已保留来源 SHA-1、实际返回副本 SHA-256、发布副本 SHA-256 和转换信息。网页副本不宣称与源文件二进制完全相同。
- 这是 2026-09-18 的阅读快照，原站后续修改需重新同步；获取、构建、发布、回退见 `web/docs/wiki-migration.md`。桌面红装鉴定、桌面反馈和静默更新不属于本次百科迁移实现。
