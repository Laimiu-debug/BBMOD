# 游戏百科中文正文 · Hub 0.3.6

2026-09-19 已发布到 https://bbmod.site/wiki/ 。游戏资料默认显示中文，可切换英文原文或中英对照；历史开发日志和公告按明确名单保留英文。

## 内容与范围

- 2,063 篇正文词条中，1,931 篇有可翻译正文的游戏资料已全文翻译并复核；118 篇官方历史日志保留英文；14 篇纯图片或空正文页面单独计数，不算未翻译。
- 包含人物出身、特长、属性、装备、敌人、世界、委托、事件、指南及玩家战报；29 个有正文的分类页也已完成。
- 64,311 个游戏文本块全部匹配译文并通过复核状态校验，未译块为 0。标题词库有 2,371 项，译文存于 `web/content/wiki-zh/` 的 22 个 JSON 文件。
- 保留 192 个主命名空间重定向、7,867 条原始版本及 5,101 个配图文件。图片总大小 254,383,722 字节。
- 保留原文版本、来源、贡献历史与原始文本下载；中文改编依 CC BY-SA 3.0 提供，媒体权利与正文许可分别说明。译文独立编写，完整原句匹配后才复用 BBMOD 自有游戏汉化。

快照：`20260919T132453Z-2d82e40d772f`。资料来源清单日期为 2026-09-18，后续原站修改需重新同步和复核。

## 中文阅读与排版

- 桌面中英对照分栏，窄屏上下排列；阅读语言随站内链接保留。
- 长表格只在自身区域横向滚动。名称列、数字区间和嵌套属性表保持可读宽度，图标按比例显示。
- 修复全站通用定义列表样式把 Wiki 说明挤入窄列、透明背景被误判为透明文字，以及内联公式产生多余滚动按钮的问题。
- 合并单元格表格保留顺序并隐藏无效排序箭头；普通独立行表格保留排序。
- 修复原站人物背景页重复锚点：保留原 ID 首次目标，为后续重复项分配不占用其他原锚点的后缀。中英对照栏也保持独立 ID。
- 中文重定向与红装别名可以检索目标词条，例如“硬化护鼻盔”和“红装弓”。纯图片页面不再误报未译。

## 发布与数据保护

在已核验的线上 `BBMOD-Hub-0.3.5-7b3098e339ca` 基础上，仅覆盖 Wiki、相关测试、说明和版本文件；打包时逐项确认其余文件与线上基线哈希一致。

发布目录：`/home/ubuntu/bbmod-hub/releases/BBMOD-Hub-0.3.6-1413d0a37df7`。

源码包共 169 个文件、9,154,216 字节，SHA-256：

`1413d0a37df7cb3fabbdd2eb62a5d1a8ce195ab61e952562c3b6677ff4daf442`

百科更新包 `wiki-20260919T132453Z-2d82e40d772f-update.tar.gz` 为 31,267,766 字节，SHA-256：

`b0dcba01e0e7fab74dea8237b0f41de36865b46b8716268ef168d666bc692ca6`

更新包复用原持久卷中内容标识完全相同的 5,101 个图片文件，只包含新快照数据。它不是含全部图片的独立恢复包；应用切换前使用 `publish_wiki --check-only` 重新验证全部快照文件及图片哈希。旧快照和原图仍保留。

升级前备份：`/home/ubuntu/bbmod-hub/backups/20260919-wiki-zh-1413d0a37df7/`。备份 SHA-256：

`b042f96227d4fb0cf4f9059d7ce9d9528e44af8b7e803dc5d0596a712feb7c30`

已验证备份数据库完整性及 19 个下载文件的大小和哈希。备份副本迁移演练没有待执行迁移，逐表比较确认所有既有行和值保持一致。

只重建 `bbmod-hub-app-1`；逐个核对其他容器 ID 和启动时间未变化，Vercel、DNS、网关配置沿用原设置。版本切换包含健康检查和发布后验收，失败会恢复旧应用与旧百科指针，不回灌旧社区数据库。

上线后核验：2 个账号、4 个托管 MOD、6 条 MOD 版本、13 条管理器版本、14 条分享种子以及 24 个探索 MOD 条目。当前管理器与汉化包的下载文件大小和 SHA-256 另行核对通过，后台仍要求登录。

## 验证证据与边界

- 隔离发布源码和 Linux 发布镜像各通过全部 140 项 Django 测试；`makemigrations --check --dry-run` 无变更。发布流程另有 4 项离线失败模拟检查。
- 完整构建及 `wiki_translation_units.py validate` 通过，数字、变量、保护节点、来源修订、适用页面与译文文件哈希全部核对。少量原站重复转义图片代码使用严格限定的例外，真实图片节点保留。
- 本地最终快照实际浏览器验证了 1,440 像素桌面和 390 像素手机宽度，涵盖装备、属性、人物背景、特长、机制、事件、战报、公式、事件锚点、中文检索和三种阅读模式。没有整页横向溢出、可见缺图或脚本异常。
- 正式域名的主页、下载、种子、建议、百科、来源说明、目录 API、中文搜索 API、装备正文 API 及新版静态资源共 11 个地址返回 200，核对中文正文与快照统计正确。
- 正式浏览器只读核对正在打开的“世界地图”页面：对应本次快照，中文正文正常，页面未溢出、没有重复 ID 和可见缺图。另一次线上自动导航等待曾超时，未把未完成的自动点击流程计为通过；完整交互流程以本地同版本验收为依据。
- 原站的过时规则和已知资料疑点保留说明，历史战报不宣称适用于当前游戏版本。原有来源缺页、外链视频及 `Bandages.png` 文件来源边界沿用前次迁移记录。

本地证据保存在 `output/wiki-translation/final-acceptance.json`、`final-validation.json`、`browser-final-*.txt`、`browser-live-observed.txt` 和 `output/deployment/wiki-translation/hub/`，生成快照和备份不提交 Git。
